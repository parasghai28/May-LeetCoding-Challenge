Solution 1: Intuitive XOR, O(n)

class Solution {
    public int singleNonDuplicate(int[] nums) {
        int ans = 0;
        for(int i = 0; i< nums.length ; i++) {
            ans = ans ^ nums[i];
        }
        return ans;
    }
}

Solution 2: Whenever O(logn) is asked and array is sorted, think about binary search

